import { NextPage } from "next";
import Navbar from "../components/navbar/Navbar";

const paymenst: NextPage = () => (
  <div className="max-w-sm mx-auto border-2 h-screen border-black">
    Payments
    <Navbar />
  </div>
);

export default paymenst;
